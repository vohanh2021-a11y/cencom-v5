# Change Password Page Design Override

**Page:** `/change-password`
**Theme:** Calm (same as Login)
**Priority:** Medium (auth flow)

## Layout

Same centered card layout as `/login`.
Form: old password + new password + confirm + submit.

## Typography & Color

Same as Login page override — centered card, gradient background, white text on form elements.

## Pre-Existing Notes

- `loading` state: button disabled + "Đang xử lý..." text
- Password match validation (client-side)
- Error near-field (below confirm field)
